<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.onimus.courseandroidkotlinudemy">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".motivation.SpotActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".motivation.SplashActivity"
            android:theme="@style/FullScreenTheme" />
        <activity
            android:name=".motivation.MotivationActivity"
            android:theme="@style/NoActionBar" />
        <activity android:name=".mytrip.MyTripActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>